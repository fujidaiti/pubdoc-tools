# Top-level Functions — categories

## capitalize

```dart
capitalize(String input) → String
```

Source: lib/categories.dart:33:36

Capitalizes [input].

```dart
String capitalize(String input) {
  if (input.isEmpty) return input;
  return input[0].toUpperCase() + input.substring(1);
}
```

---

