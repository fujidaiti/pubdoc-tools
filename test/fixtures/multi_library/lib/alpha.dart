/// The alpha library.
library alpha;

/// A greeting class.
class Greeter {
  /// The name.
  final String name;

  /// Creates a [Greeter].
  Greeter(this.name);
}

/// Says hello.
String hello() => 'hello';
