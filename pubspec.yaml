name: dartdoc_txt
description: A CLI tool that generates Markdown documentation from Dart/Flutter packages for LLM consumption.
version: 0.0.1

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.7.0
  dartdoc:
    path: dartdoc
  path: ^1.8.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
